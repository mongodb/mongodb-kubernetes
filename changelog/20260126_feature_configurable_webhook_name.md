---
kind: feature
date: 2026-01-26
---

* Added support for configurable ValidatingWebhookConfiguration name via `operator.webhook.name` helm value, allowing multiple operator instances on the same cluster.

