{
    "id": "pyenv",
    "version": "1.0.0",
    "name": "pyenv",
    "description": "Installs pyenv and its system build dependencies.",
    "options": {
        "pythonVersion": {
            "type": "string",
            "default": "none",
            "description": "The default Python version hint."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
