---
apiVersion: mongodb.com/v1
kind: MongoDB
metadata:
  name: workload-mdb
spec:
  members: 3
  version: "8.0.6-ent"
  type: ReplicaSet
  opsManager:
    configMapRef:
      name: workload-mdb-config
  credentials: my-credentials
  persistent: true
  backup:
    mode: enabled
