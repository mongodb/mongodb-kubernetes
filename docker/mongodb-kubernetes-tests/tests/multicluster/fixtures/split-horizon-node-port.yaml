---
apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    controller: mongodb-kubernetes-operator
spec:
  type: NodePort
  selector:
    controller: mongodb-kubernetes-operator
  ports:
    - port: 27017
      targetPort: 27017
      nodePort: 30007
