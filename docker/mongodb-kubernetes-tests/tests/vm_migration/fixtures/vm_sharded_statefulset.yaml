---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vm-sharded-mongod
spec:
  serviceName: vm-sharded-mongod
  replicas: 6
  selector:
    matchLabels:
      app: vm-sharded-mongod
  template:
    metadata:
      labels:
        app: vm-sharded-mongod
    spec:
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
      containers:
        - name: mongodb-agent
          image: quay.io/mongodb/mongodb-agent:13.52.0.10711-1
          ports:
            - containerPort: 27017
              name: mongodb
          command:
            - /bin/sh
            - -c
            - ./agent/mongodb-agent -mmsGroupId=$(MMS_GROUP_ID) -mmsBaseUrl=$(MMS_BASE_URL) -mmsApiKey=$(MMS_API_KEY)
          env:
            - name: MMS_GROUP_ID
              value: ""
            - name: MMS_BASE_URL
              value: ""
            - name: MMS_API_KEY
              value: ""
          volumeMounts:
            - name: mongodb-data
              mountPath: /data
#            - name: mongodb-certs
#              mountPath: /etc/mongodb/certs
#              readOnly: true
#      volumes:
#        - name: mongodb-certs
#          secret:
#            secretName: mdb-certs

  volumeClaimTemplates:
    - metadata:
        name: mongodb-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
