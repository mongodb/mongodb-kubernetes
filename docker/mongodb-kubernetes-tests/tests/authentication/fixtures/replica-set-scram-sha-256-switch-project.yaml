---
apiVersion: mongodb.com/v1
kind: MongoDB
metadata:
  name: replica-set-scram-sha-256-switch-project
spec:
  members: 3
  version: 4.4.0
  type: ReplicaSet
  opsManager:
    configMapRef:
      name: my-project
  credentials: my-credentials
  logLevel: DEBUG
  persistent: false
  security:
    authentication:
      enabled: true
      modes: ["SCRAM"]
