apiVersion: mongodb.com/v1
kind: MongoDBOpsManager
metadata:
  name: om-scale
spec:
  version: 4.2.0-ubuntu
  adminCredentials: ops-manager-admin

  applicationDatabase:
    members: 3
    version: 4.0.7
    persistent: false
    type: ReplicaSet
    podSpec:
      cpu: '0.25'
