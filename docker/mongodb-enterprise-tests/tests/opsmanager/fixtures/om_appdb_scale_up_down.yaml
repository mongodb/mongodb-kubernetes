apiVersion: mongodb.com/v1
kind: MongoDBOpsManager
metadata:
  name: om-scale
spec:
  replicas: 1
  version: 4.2.0
  adminCredentials: ops-manager-admin-secret

  applicationDatabase:
    members: 3
    version: 4.0.7
    persistent: false
    podSpec:
      cpu: '0.25'

  backup:
    enabled: false
