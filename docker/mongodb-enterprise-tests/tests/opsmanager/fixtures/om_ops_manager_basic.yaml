apiVersion: mongodb.com/v1
kind: MongoDBOpsManager
metadata:
  name: om-basic
spec:
  replicas: 1
  version: 4.2.4
  adminCredentials: ops-manager-admin-secret

  applicationDatabase:
    members: 3
    version: 4.2.0

  backup:
    enabled: false
