apiVersion: mongodb.com/v1
kind: MongoDBOpsManager
metadata:
  name: om-basic
spec:
  replicas: 1
  version: 4.4.0
  adminCredentials: ops-manager-admin-secret

  applicationDatabase:
    members: 3
    version: 4.2.2-ent
    persistent: true

  backup:
    enabled: false
