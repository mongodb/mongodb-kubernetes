apiVersion: mongodb.com/v1
kind: MongoDBOpsManager
metadata:
  name: om-scram
spec:
  replicas: 1
  version: 4.2.0
  adminCredentials: ops-manager-admin-secret

  applicationDatabase:
    members: 3
    version: 4.0.0
    persistent: true
