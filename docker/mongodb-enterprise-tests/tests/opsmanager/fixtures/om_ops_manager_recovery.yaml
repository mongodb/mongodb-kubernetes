apiVersion: mongodb.com/v1
kind: MongoDBOpsManager
metadata:
  name: om-recovery
spec:
  version: 4.2.0.56506.20190804T0058Z-1
  adminCredentials: ops-manager-admin

  applicationDatabase:
    members: 3
    version: 100.0.0 # some fake version to make sure appdb doesn't start
    persistent: false
    type: ReplicaSet
