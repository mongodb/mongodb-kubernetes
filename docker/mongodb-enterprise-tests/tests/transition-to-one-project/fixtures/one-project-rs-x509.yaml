---
apiVersion: mongodb.com/v1
kind: MongoDB
metadata:
  name: one-project-transition
spec:
  members: 3
  version: 3.6.17
  type: ReplicaSet
  project: my-project
  credentials: my-credentials
  persistent: false
  security:
    tls:
      enabled: true
    authentication:
      enabled: true
      modes: ["X509"]
