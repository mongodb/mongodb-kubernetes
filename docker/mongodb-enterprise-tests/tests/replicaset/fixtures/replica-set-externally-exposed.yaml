apiVersion: mongodb.com/v1
kind: MongoDB
metadata:
  name: my-replica-set-externally-exposed
spec:
  members: 1
  version: 4.2.0-rc8 # todo change to 4.2 when it's released
  type: ReplicaSet
  project: my-project
  credentials: my-credentials
  persistent: false
  exposedExternally: true
