---
apiVersion: mongodb.com/v1
kind: MongoDB
metadata:
  name: my-invalid-replica-set
spec:
  members: 3
  version: 10.0.0
  type: ReplicaSet
  opsManager:
    configMapRef:
      name: my-project
  credentials: my-credentials
  logLevel: DEBUG
  persistent: false
