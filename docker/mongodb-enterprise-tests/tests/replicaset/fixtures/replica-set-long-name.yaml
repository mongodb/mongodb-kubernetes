---
apiVersion: mongodb.com/v1
kind: MongoDB
metadata:
  name: my-replica-set-has-exactly-52-characters-in-its-name
spec:
  members: 3
  version: 3.6.9
  type: ReplicaSet
  opsManager:
    configMapRef:
      name: my-project
  credentials: my-credentials
  logLevel: DEBUG
