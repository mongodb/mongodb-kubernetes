# Version Definitions
# Centralized version specifications for all components

variables:
  # Ops Manager Version Anchors
  # IMPORTANT: The order/index is important, since these are anchors. Please do not change
  - &ops_manager_60_latest 6.0.27
  - &ops_manager_70_latest 7.0.15
  - &ops_manager_80_latest 8.0.7

  # Version update documentation:
  # To update Ops Manager versions:
  # 1. Update the version number above
  # 2. Ensure corresponding build variants are updated
  # 3. Test with a patch build before merging
  # 4. Coordinate with team for any breaking changes
