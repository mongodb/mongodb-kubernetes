---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: mongodb-enterprise-ops-manager
  name: ops-manager-internal
spec:
  selector:
    app: mongodb-enterprise-ops-manager
  ports:
  - protocol: TCP
    port: 8080

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: mongodb-enterprise-ops-manager
  name: mongodb-enterprise-ops-manager
spec:
  replicas: 1
  serviceName: ops-manager
  selector:
    matchLabels:
      app: mongodb-enterprise-ops-manager
  template:
    metadata:
      labels:
        app: mongodb-enterprise-ops-manager
    spec:
      securityContext:
        fsGroup: 1000080000
        supplementalGroups: [0, 1000080000]
      containers:
      - name: mongodb-enterprise-ops-manager
        image: 268558157000.dkr.ecr.us-east-1.amazonaws.com/dev/mongodb-enterprise-ops-manager:4.0.2.50187.20180905T1427Z-1
        imagePullPolicy: IfNotPresent
        readinessProbe:
          httpGet:
            port: 8080
            path: /
          initialDelaySeconds: 30
          timeoutSeconds: 1
        env:
        - name: OM_HOST
          value: ops-manager-internal
        volumeMounts:
        - name: mongodb-mms-data
          mountPath: /data
        - name: mongodb-mms-config
          mountPath: /etc/mongodb-mms
        resources:
          requests:
            memory: "4Gi"
  volumeClaimTemplates:
  - metadata:
      name: mongodb-mms-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 20Gi
  - metadata:
      name: mongodb-mms-config
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 10Mi
